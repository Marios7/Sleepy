<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:notify="http://www.hardcodet.net/taskbar"
    mc:Ignorable="d"
    Title="Sleepy"
    >

    <Grid x:Name="RootGrid" Background="#fffaec"   Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition   Height="Auto"/>
            <RowDefinition  Height="120" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,40,0,0" HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">

            <!-- Application Icon and Name -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,20,0,20">
                <TextBlock Text="Sleepy" FontSize="24" FontWeight="ExtraLight"  VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Time Input -->
            <!--<StackPanel Orientation="Vertical" Spacing="10" Margin="0,0,0,20">
                <TextBox x:Name="StartAfterTimeTextBox"  Header="Idle Time threshold:" PlaceholderText="Time" Width="300" PreviewKeyDown="IdleTimeTextBox_PreviewKeyDown" TextChanged="IdleTimeTextBox_TextChanged"/>
            </StackPanel>-->
            <Border Height="1" Background="#D3D3D3" Margin="0,0"/>


            <Grid VerticalAlignment="Center" Margin="0,10,0,0">
                <CheckBox x:Name="StartUp_CheckBox" Content="Start on Windows startup" Margin="0,5,0,0" Checked="StartUp_CheckBox_Checked" Unchecked="StartUp_CheckBox_Unchecked"/>
                <Button x:Name="Apply_Button" HorizontalAlignment="Right" FontSize="12" Content="Apply" Width="100" Height="30" BorderThickness="1" Background="White" Foreground="Black" Click="ApplyButton_Click"/>
            </Grid>

            <Border Height="1" Background="#D3D3D3" Margin="0,10"/>

            <StackPanel Orientation="Vertical" Margin="0,10,0,0">
                <CheckBox HorizontalAlignment="Left" x:Name="MoveMouse_CheckBox" Content="Move the mouse" Margin="0,2,0,0" IsChecked="true"/>
                <CheckBox HorizontalAlignment="Left" x:Name="ActivateAnimation_CheckBox" IsChecked="True" Content="Activate animation" Margin="0,2,0,0" Checked="ActivateAnimation_CheckBox_Checked" Unchecked="ActivateAnimation_CheckBox_Unchecked"/>
                <!--<ToggleSwitch x:Name="ThemeSlider" Toggled="ThemeSlider_Toggled" HorizontalAlignment="Left" VerticalAlignment="Center" Width="60" Height="30" OnContent="Dark" OffContent="Light"></ToggleSwitch>-->
            </StackPanel>


            <Border Height="1" Background="#D3D3D3" Margin="0,10"/>
            <!-- Start and Stop Buttons -->
            <Grid Margin="0,10,0,0">
                <Button HorizontalAlignment="Left" Content="Start" Width="100" Height="35" BorderBrush="Green" BorderThickness="1" Background="White" Foreground="Black" Click="StartButton_Click"/>
                <Button HorizontalAlignment="Right" Content="Stop" Width="100" Height="35" BorderBrush="Red" BorderThickness="1" Background="White" Foreground="Black" Click="StopButton_Click"/>

            </Grid>


        </StackPanel>
        <Grid Grid.Row="1" Width="300" Margin="0,20,0,0">



            <WebView2 Grid.Row="0" x:Name="GifView"
       Source=""
       Visibility="Visible"
       Width="100"
       Height="100"
       HorizontalAlignment="Center"
       VerticalAlignment="Center"
       
         />


        </Grid>



    </Grid>
</Window>
